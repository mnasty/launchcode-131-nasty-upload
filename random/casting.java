
public class casting {

	
	
	double casting (double)23 / 24;
	
	//use this to cast a number as a double so that you will get a real answer and not 
	//get cut off with int (single digit) math
	
	double math = Math.round(3.14159254 / 100.0);
	
	//use this type of setup to use math.round to decimal point precision on a 
	//single line rather than in systemout or creating an unneccisary variable
	//and feeding into it.
	
}

